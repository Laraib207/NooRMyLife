# MongoDB Atlas Connection String
# Replace with your actual MongoDB Atlas connection string
# Get it from: https://cloud.mongodb.com/ (Database > Connect > Connect your application)
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/portfolio?retryWrites=true&w=majority

# Note: Replace 'username', 'password', and 'cluster' with your actual MongoDB Atlas credentials
# Make sure to add your IP address to the whitelist in MongoDB Atlas
